/* COMPOSE BIT STRING*/
xnap_return_et e2ap_compose_{{name}} (OSCTXT* asn1_ctx, e2ap_{{name}}* pvalue){

    UInt16 numbits = 32; //--------->dungnm23 sửa số bit ở đây
    OSOCTET src[4] = {0x12, 0x34, 0xAB, 0xCD}; //--------->dungnm23 sửa giá trị bit ở đây

    // BIT STRING  size(a..b,...)
    pvalue->numbits = numbits;
    XNAP_MEMCPY((void*)pvalue->data, src, (numbits+7)/8);

    gnb_return_t retVal = NR_FAILURE;
    
    //XNAP_TRACE(XNAP_INFO,"dungnm23 - %s: Compose e2ap_{{name}} success with value %d",__FUNCTION__,*pvalue);
    retVal = NR_SUCCESS;
    return retVal;
}
